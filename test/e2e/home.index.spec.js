var util = require('util');


describe("starter", function () {

  describe("menu.home", function () {
    //var ptor;

    beforeEach(function(){
      //ptor = protractor.getInstance();
    });

    it("should display the correct title", function () {
      //ptor.get('#/');
      //ptor.findElement(protractor.By.className('body')).isElementPresent().then(function(v){
        expect(1).toBe(1);
        //expect(v).toBe(true);
      //});
    });

  });
});